apiVersion: v1
kind: Service
metadata:
  name: {{ include "gw-dms-mol-portal.fullname" . }}
  labels:
    {{- include "gw-dms-mol-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gw-dms-mol-portal.selectorLabels" . | nindent 4 }}
